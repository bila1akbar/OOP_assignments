#include <iostream>
#include <bitset>
#include <string.h>
using namespace std;

int main()
{
    
    // string name = "Bilal";
    // int id = 14;
    // string temp = " ";
    // temp = temp + "Name : ";
    // temp = temp + name;
    // int length = 1;
    // int x = id;
    // while (x /= 10)
    // length++;
    // char *storing_id = new char[length];
    // x = id;
    // int i = 0;
    // int result = 0;

	// while (x > 0) {
	// 	result = result * 10 + x % 10;
    //     storing_id[i]=result + 48;
	// 	x /= 10;
    //     i++;
    //     result=0;
	// }
    // temp = temp + " Id : ";
    // for (int j = i - 1; j >= 0; j--)
    //     temp = temp + storing_id[j];
    // delete[] storing_id;
    // cout << temp;

    return 0;
}